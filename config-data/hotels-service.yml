server:
  port: 8082

spring:
  application:
    name: hotels-service
  datasource:
    url: jdbc:mysql://localhost:3306/hotels_service?serverTimezone=UTC
    username: root
    password: "1234"
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka